import { Controller } from "@vigilio/express-core";
@Controller("/ticket")
export class TicketApiController {
	// @Get("/:code")
	// print(@Params(":code") code: string, @Res() res: Response) {
	//     if (fs) {
	//     }
	// }
}
